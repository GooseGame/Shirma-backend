name: Deploy to Hosting

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          composer install --no-dev --optimize-autoloader
          sync

      - name: Create deployment archive
        run: |
          tar -czf deploy.tar.gz \
            core/ \
            includes/ \
            public/ \
            vendor/ \
            composer.json \
            composer.lock \

          ls -lh deploy.tar.gz

      - name: Upload archive to server
        uses: appleboy/scp-action@v0.1.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "deploy.tar.gz"
          target: "/home/c/${{ secrets.SERVER_USER }}/"
          rm: true

      - name: Extract and deploy
        uses: appleboy/ssh-action@v0.1.4
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script_timeout: 15m
          script: |
            DEPLOY_DIR="/home/с/${{ secrets.SERVER_USER }}"
            
            tar -xzf "$DEPLOY_DIR/deploy.tar.gz" -C "$DEPLOY_DIR"
            
            rm -rf "$DEPLOY_DIR/public_html"  # Удаляем старую версию
            mv "$DEPLOY_DIR/public" "$DEPLOY_DIR/public_html"
            
            find "$DEPLOY_DIR" -type d -exec chmod 755 {} \;
            find "$DEPLOY_DIR" -type f -exec chmod 644 {} \;
            chmod -R 755 "$DEPLOY_DIR/public_html"
            
            rm -f "$DEPLOY_DIR/deploy.tar.gz"

      - name: Create .env
        uses: appleboy/ssh-action@v0.1.4
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            DEPLOY_DIR="/home/c/${{ secrets.SERVER_USER }}"
            cat > "$DEPLOY_DIR/.env" <<EOF
            DB_HOST=${{ secrets.DB_HOST }}
            DB_NAME=${{ secrets.DB_NAME }}
            DB_USER=${{ secrets.DB_USER }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            GOOGLE_CLIENT_ID=${{secrets.GOOGLE_CLIENT_ID}}
            GOOGLE_CLIENT_SECRET=${{secrets.GOOGLE_CLIENT_SECRET}}
            JWT_ACCESS_SECRET=${{secrets.JWT_ACCESS_SECRET}}
            JWT_REFRESH_SECRET=${{secrets.JWT_REFRESH_SECRET}}
            EOF
            chmod 600 "$DEPLOY_DIR/.env"